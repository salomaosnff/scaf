{
  "name": "<%= project.name %>",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"<% if (useEslint) { %>,
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs --fix"<% } %><% if (usePrettier) { %>,
    "format": "prettier --write src/"<% } %>
  }
}
