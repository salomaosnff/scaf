{
  "name": "<%= project.name %>",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview"<% if (useEslint) { %>,
    "lint": "eslint . --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx --fix"<% } %><% if (usePrettier) { %>,
    "format": "prettier --write src/"<% } %>
  }
}